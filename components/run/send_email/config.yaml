###
### Run Component for sending emails
###
# -None

# --Required resource files--   
# relative to components folder (so ../../ to return to base snakemake folder) if not an absolute path
# -None

# Requirements for running component
requirements:
  # None